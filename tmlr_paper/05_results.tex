\section{Results}

\subsection{Transformer Results}

The GPT-2-style transformer exhibits clear superposition patterns when processing synthetic sparse features.

\paragraph{Feature geometry (Figure~\ref{fig:transformer_features}).} We visualize feature embeddings from the input projection layer across multiple random instances. Despite different input patterns, features consistently arrange themselves with approximately uniform angular spacing---the same geometric organization observed in toy autoencoders. This suggests the transformer has learned to minimize interference by distributing features evenly in the representational space.

\paragraph{Effect of sparsity (Figure~\ref{fig:feature_analysis}).} The upper panels show feature norms (bar height) and polysemanticity (color intensity, green=low, yellow=high) for models trained with different sparsity levels. At low sparsity (0.070, features frequently active), the first $\sim$64 features (up to the hidden dimension, marked by dashed line) have high norms and low polysemanticity---they occupy dedicated dimensions. Features beyond this boundary have lower norms and higher polysemanticity, indicating superposition. At high sparsity (0.684, features rarely active), more features participate in superposition, with polysemanticity spread more evenly across features. The model exploits sparsity to represent more features.

The lower panels show weight correlation matrices ($\mathbf{W}^\top \mathbf{W}$), revealing feature interactions. Higher sparsity produces denser correlation patterns, indicating more feature sharing.

\paragraph{Dimensional fractions (Figure~\ref{fig:geometric_analysis}).} We analyze what fraction of a dimension each feature occupies. Features cluster around theoretically significant fractions ($1/2$, $2/3$, $3/4$), as predicted by the original paper's analysis of optimal feature packing. This clustering becomes more pronounced at higher inverse sparsity (more superposition), suggesting systematic organization rather than random interference.

% Figure for transformer features
\begin{figure}[t]
    \centering
    \includegraphics[width=\textwidth]{figures/transformer_features.png}
    \caption{\textbf{Feature embeddings from the GPT-2-style transformer.} Each panel shows feature organization for a different input instance. Despite varying inputs, features maintain consistent geometric arrangements with approximately uniform angular spacing, demonstrating stable superposition patterns across the transformer architecture.}
    \label{fig:transformer_features}
\end{figure}

% Figure for feature analysis
\begin{figure*}[t]
    \centering
    \includegraphics[width=\textwidth]{figures/interference_heatmap.png}
    \caption{\textbf{Feature analysis across superposition models.} \textbf{Top two panels (Sparsity 0.070):} Low sparsity setting. Upper: Feature norms (bar height) and polysemanticity (color: green=low, yellow=high). The dashed vertical line marks the hidden dimension ($d=64$); features to the right must be superposed. Lower: Weight correlation matrix ($\mathbf{W}^\top \mathbf{W}$) showing feature interactions. \textbf{Bottom two panels (Sparsity 0.684):} High sparsity setting shows more widespread superposition and denser correlations.}
    \label{fig:feature_analysis}
\end{figure*}

% Figure for geometric analysis
\begin{figure}[t]
    \centering
    \includegraphics[width=\textwidth]{figures/phase_diagram.png}
    \caption{\textbf{Phase diagram of mean feature dimensionality.} X-axis: feature probability $p = 1 - S$ (inverse sparsity). Y-axis: relative importance. Color: mean dimensionality $\|\mathbf{w}\|^2$. Yellow (top-left) indicates features with dedicated dimensions ($\|\mathbf{w}\|^2 \approx 1$); blue-purple indicates features dropped from representation. The sharp boundary between regimes is consistent with the critical sparsity threshold of Corollary~\ref{cor:sparsity}: superposition is beneficial only when $S > 1 - d\tau/(n-d)^2$.}
    \label{fig:geometric_analysis}
\end{figure}

\subsection{Translation Model Results}

The bottleneck translation model demonstrates that superposition enables practical performance under severe capacity constraints.

\paragraph{Translation quality.} Despite compressing 512-dimensional encoder outputs to just 16 dimensions, the model achieves a BLEU score of 36.34 on newsdiscussdev2015-enfr, compared to 33.8 for the baseline model without bottleneck. The bottleneck did not impair---and slightly improved---translation quality, suggesting efficient information compression through superposition.

\paragraph{Weight distribution.} The bottleneck layer weights exhibit a peaked distribution with symmetric tails, indicating the model learned structured, efficient encoding rather than random projections.

\paragraph{Embedding space visualization (Figure~\ref{fig:translation_embeddings}).} t-SNE visualization of the 256-dimensional bottleneck space reveals strong clustering by part-of-speech category. Nouns (pink), verbs (orange), adjectives (gold), adpositions (teal), conjunctions (blue), and pronouns (purple) each form distinct regions, with numerals and punctuation isolated at the periphery. This POS-structure emerges without any syntactic supervision, driven entirely by the next-token prediction objective under bottleneck compression.

This organization is theoretically significant: part-of-speech categories define a natural sparsity structure, since tokens of the same POS rarely co-occur in the same syntactic slot. The bottleneck space thus learns to exploit this sparsity, consistent with the prediction that $P_{ij} \approx 0$ for tokens of distinct POS categories, enabling their superposition in the same dimensions.

% Figure for translation embeddings
\begin{figure}[t]
    \centering
    \includegraphics[width=\textwidth]{figures/embeddings_pos_colored.png}
    \caption{\textbf{Bottleneck embeddings colored by part-of-speech.} t-SNE projection of $d=256$ bottleneck representations from the translation model, colored by Universal POS tag. Distinct POS categories cluster into separate regions without syntactic supervision, demonstrating that the model learns to organize linguistically structured features in the compressed space. Nouns (pink circles) and verbs (orange crosses) occupy the largest regions; function word categories (DET, CONJ, PRT) are tightly grouped, reflecting their limited distributional variety.}
    \label{fig:translation_embeddings}
\end{figure}

\subsection{Sparsity Governs Interference: Validating the Unified Loss}

Figure~\ref{fig:interference_sparsity} shows the Gram matrix $G_{ij} = \langle \mathbf{w}_i, \mathbf{w}_j \rangle$ at two extremes of feature activation probability. At $p = 1.0$ (no sparsity, $S = 0$), the off-diagonal entries span the full range $[-1, 1]$, with features forced into strongly correlated or anti-correlated encodings. At $p = 0.05$ (high sparsity, $S \approx 0.998$), the same $d$-dimensional model encodes the same $n$ features with substantially reduced and more structured interference.

This is the central empirical prediction of Theorem~\ref{thm:equivalence}: because $\mathcal{L}_{\text{int}} = \sum_{i \neq j} P_{ij} G_{ij}^2$ and $P_{ij} = p^2$ in the representational setting, reducing $p$ directly reduces the co-occurrence weight on each interfering pair. The model responds by finding encodings where $G_{ij}^2$ can be nonzero for more pairs, representing more features in superposition at the same interference budget $\tau$.

\begin{figure}[t]
    \centering
    \begin{subfigure}[b]{0.48\textwidth}
        \includegraphics[width=\textwidth]{figures/interference_instance_0_p1.000.png}
        \caption{$p = 1.00$ ($S = 0$): no sparsity.}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.48\textwidth}
        \includegraphics[width=\textwidth]{figures/interference_instance_9_p0.050.png}
        \caption{$p = 0.05$ ($S \approx 0.998$): high sparsity.}
    \end{subfigure}
    \caption{\textbf{Interference heatmaps at opposite sparsity extremes.} Each cell shows $G_{ij} = \langle \mathbf{w}_i, \mathbf{w}_j \rangle$ for a pair of feature vectors. At full density ($p=1$), interference is large and chaotic---features cannot find near-orthogonal encodings under maximal co-occurrence pressure. At high sparsity ($p=0.05$), the same model finds a structured encoding where interference is controlled. This validates the dependence of $\mathcal{L}_{\text{int}}$ on $P_{ij} = p^2$ (Theorem~\ref{thm:equivalence}).}
    \label{fig:interference_sparsity}
\end{figure}

\subsection{Representational and Reasoning Models Converge to the Same Geometry}

Figure~\ref{fig:interference_comparison} shows interference heatmaps for the \texttt{continuous\_thought} and \texttt{coconut} models, both at $d = 256$. The two heatmaps are visually indistinguishable: both show near-zero off-diagonal cosine similarity across all feature pairs, with the diagonal at 1.0.

This convergence is the core empirical evidence for Theorem~\ref{thm:equivalence}. Despite being trained on qualitatively different objectives---feature reconstruction vs.\ next-token prediction with continuous thought---both models arrive at near-orthogonal feature geometries under the same bottleneck dimension. The unified loss $\mathcal{L}(\mathbf{W}, P)$ predicts exactly this: optimal encodings minimize $\mathcal{L}_{\text{int}}$ subject to the capacity constraint, and the resulting geometry depends on $d$ and $S(P)$, not on the surface form of the training task.

\begin{figure}[t]
    \centering
    \begin{subfigure}[b]{0.48\textwidth}
        \includegraphics[width=\textwidth]{figures/interference_heatmap_continuous_thought.png}
        \caption{\texttt{continuous\_thought}, $d=256$.}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.48\textwidth}
        \includegraphics[width=\textwidth]{figures/interference_heatmap_coconut.png}
        \caption{\texttt{coconut}, $d=256$.}
    \end{subfigure}
    \caption{\textbf{Both model families converge to the same feature geometry.} Interference heatmaps ($G_{ij} = \langle \mathbf{w}_i, \mathbf{w}_j \rangle$) for the continuous thought and Coconut models at $d=256$. Both show near-zero off-diagonal similarity, indicating near-orthogonal encodings. The visual indistinguishability of the two heatmaps provides direct evidence for Theorem~\ref{thm:equivalence}: representational and reasoning superposition minimize the same unified loss, producing the same optimal geometry.}
    \label{fig:interference_comparison}
\end{figure}

\subsection{Superposition Emerges Gradually During Training}

Figure~\ref{fig:weight_progression} shows the evolution of feature weight vectors across training, from random initialization (step 0) to convergence (step 14,900). At initialization, features cluster near the origin with small, unsystematic directions. By step 5,000, features begin spreading toward unit norm. By step 14,900, features are distributed with approximately uniform angular spacing---the equiangular structure predicted by the Welch bound (Theorem~\ref{thm:welch}) as the tight-frame solution that minimizes $\sum_{i \neq j} G_{ij}^2$ given the capacity constraint.

This progression has two implications. First, superposition is a learned strategy, not a random artifact: the model actively organizes features geometrically to minimize the unified loss. Second, the convergence toward equiangular structure supports the tightness of our capacity bounds (Corollary~\ref{cor:tight}): the model converges to approximately the optimal packing, consistent with the ETF construction used in the lower bound proof.

\begin{figure}[t]
    \centering
    \begin{subfigure}[b]{0.48\textwidth}
        \includegraphics[width=\textwidth]{figures/weights_step_0.png}
        \caption{Step 0: random initialization.}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.48\textwidth}
        \includegraphics[width=\textwidth]{figures/weights_step_14900.png}
        \caption{Step 14,900: near convergence.}
    \end{subfigure}
    \caption{\textbf{Emergence of superposition during training.} Feature weight vectors shown at initialization and near convergence. At step 0, features are small and unsystematic. By step 14,900, features are distributed with approximately uniform angular spacing across instances, converging toward the equiangular tight frame structure predicted by the Welch bound. Each panel in the strip represents a different training instance; the consistency across instances indicates this geometry is a stable attractor of the loss landscape.}
    \label{fig:weight_progression}
\end{figure}

\subsection{Feature Dimensionality Transitions}

Figure~\ref{fig:feature_dim_curves} shows feature dimensionality $\|\mathbf{w}_i\|^2$ as a function of feature activation probability $p$ for five features at fixed importance $\approx 0.34$. Several aspects of these curves directly validate the theoretical predictions.

First, features undergo \emph{discontinuous transitions}: dimensionality drops sharply from $\approx 1.3$ to $0$ rather than decreasing smoothly. This is consistent with the sharp phase boundary visible in Figure~\ref{fig:geometric_analysis} and with Corollary~\ref{cor:critical}: once $p$ exceeds the critical threshold $p^* = \sqrt{1 - d\tau/(n-d)^2}$, the interference cost of maintaining a feature in superposition exceeds the capacity benefit, and the model drops it entirely.

Second, features are dropped in \emph{priority order}: Feature 0 (lowest importance) drops first at $p \approx 0.03$, while higher-importance features persist to larger $p$ values. This is the importance-weighting effect predicted by $\mathcal{L}_{\text{cap}} = \sum_i p_i(1 - \|\mathbf{w}_i\|^2)^2$: the optimizer maintains features whose contribution to $\mathcal{L}_{\text{cap}}$ is large, dropping low-importance features when their interference cost becomes prohibitive.

Third, dimensionality values cluster near $\|\mathbf{w}\|^2 \approx 1.3 > 1$. Norms exceeding 1.0 reflect the role of the capacity loss $\mathcal{L}_{\text{cap}}$ pulling features toward unit norm, with the slight excess arising from the importance weighting $\omega_i > 1$.

\begin{figure}[t]
    \centering
    \includegraphics[width=\textwidth]{figures/feature_dim_curves.png}
    \caption{\textbf{Feature dimensionality as a function of activation probability.} Each curve tracks $\|\mathbf{w}_i\|^2$ for one of five features as $p$ varies from $10^{-2}$ to $1$ (x-axis in log scale). The dashed line at 1.0 is the full-dimension reference. Features drop discontinuously from $\|\mathbf{w}\|^2 \approx 1.3$ to $0$ as $p$ increases, consistent with the critical sparsity threshold (Corollary~\ref{cor:critical}). Lower-importance features (Feature 0, dark purple) drop at smaller $p$, confirming that the optimizer prioritizes high-importance features under interference pressure.}
    \label{fig:feature_dim_curves}
\end{figure}

\subsection{Polysemanticity in a Practical Language Model}

The max-activating token analysis of the $d=256$ bottleneck model reveals that 197 of 256 neurons (77\%) are polysemantic---they activate maximally for tokens from multiple unrelated semantic categories. This polysemanticity rate is consistent with the capacity bound: $n_{\text{eff}} \leq d + O(d^2 S / \tau)$ predicts that at $d=256$ with the sparsity structure of natural language, a large fraction of neurons must share representations across multiple features.

The remaining monosemantic neurons show meaningful specialization: Neuron 1 fires exclusively for \textit{proposal}/\textit{answer} (discourse-structural), Neuron 3 for modal verbs (\textit{will}, \textit{would}), Neuron 5 for \textit{Parliament} (domain-specific), and Neuron 8 for \textit{common}/\textit{single} (quantificational). These monosemantic neurons occupy dedicated dimensions, while the polysemantic majority implement superposition to pack the vocabulary's full feature space into 256 dimensions.

\subsection{Summary}

Table~\ref{tab:comparison} summarizes evidence across all four experimental settings.

\begin{table}[t]
\centering
\caption{Superposition across architectures and objectives. All settings show superposition governed by sparsity, converging to the same geometric structure predicted by the unified loss $\mathcal{L}(\mathbf{W}, P)$.}
\label{tab:comparison}
\begin{tabular}{lcccc}
\toprule
\textbf{Aspect} & \textbf{Toy Autoencoder} & \textbf{GPT-2} & \textbf{Translation} & \textbf{Coconut} \\
\midrule
Architecture    & Single layer + ReLU & 4-layer transformer & Encoder-decoder & Iterative thought \\
Objective       & Feature reconstruction & Feature reconstruction & BLEU & Next-token \\
$P_{ij}$ structure & $p_i p_j$ & $p_i p_j$ & Co-occurrence & Trace co-validity \\
Evidence        & Geometric patterns & Angular spacing & POS clusters & Same heatmap as CT \\
Polysemanticity & High at small $d$ & High at small $d$ & 77\% at $d=256$ & Near-zero $\mu$ at $d=256$ \\
Governed by     & Sparsity, importance & Sparsity, importance & Linguistic statistics & Trace structure \\
\bottomrule
\end{tabular}
\end{table}
